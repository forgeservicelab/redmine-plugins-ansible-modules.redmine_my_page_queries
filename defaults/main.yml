---
# defaults file for redmine_backlogs_ansible

redmine_dir: /opt/redmine
RAILS_ENV: production
sudo_user: redmine

redmine_backlogs:
  task_tracker: Task
  story_trackers:
    - Bug
    - Story
    - Risk
    - Change
    - Incident
